# 长篇生产 P0 交接：状况、结果、改动、下一步（2026-09-13 晚）

> **2026-09-14 更新：** P0 三片 + Obsidian 导入分隔已快进合入 `feature/skill-engine`（`81856da..6ff75e1`，文档收口 `8a622f5`）。合入当时未 push，现已在 `origin/feature/skill-engine`。终审补录见 `docs/superpowers/plans/2026-09-14-p0-final-review.md`。下文是合入前的历史交接，保留作档案。P1 仍未授权。
>
> **给下一位开发者：** 先读本文，再读合同与短计划。不要按冻结讨论稿第 1～11 节编码。不要在共享 checkout `D:\ccx` 里写 P0 代码。
>
> **合同：** `docs/superpowers/specs/2026-09-13-ai-production-current-contract.md`  
> **短计划：** `docs/superpowers/plans/2026-09-13-p0-provider-planning-preview.md`  
> **计划审查：** `docs/superpowers/plans/2026-09-13-p0-provider-planning-preview-review.md`  
> **讨论档案（已冻结）：** `docs/superpowers/plans/2026-09-13-ai-long-novel-production-architecture-plan.md`

---

## 一句话状况

P0 **已批准并已开工，但没有做完，也没有合回主工作区。**  
独立 worktree 里 **Provider 片已提交并过双重审查**；**Planning 片代码大半已写、未提交、未审查**；**Preview 片与整分支最终门未开始**。P1 仍未授权。

---

## 1. 今天做了什么（文档轨 vs 代码轨）

同一天有两条线，不要混在一起看。

### 1.1 文档轨（在共享 `D:\ccx`，目前都是未跟踪文件）

三位开发者把长篇生产讨论稿收敛成可施工合同：

| 产物 | 状态 |
|------|------|
| 讨论稿冻结：禁止按 1～11 节编码，停止追加第 21 节 | 已落地 |
| 当前合同（P0 三件 + 禁止项 + 验收命令） | 已落地，效力高于讨论稿 |
| P0 短计划（Provider → Planning → Preview 三片顺序） | 已按审查第七节默认值改写 |
| P0 计划审查（含 7.8 修订落地、第八节 Task 1 核对） | 已落地 |

拍板过的执行方式：

- 选 **Subagent-Driven**
- 独立 git worktree，**共享 `D:\ccx` 不写 P0 代码**
- 三片顺序 commit、不 push
- 每片「规格符合性 + 代码质量」双重审查
- **全部过关后再问是否合并**（合并不自动发生）

### 1.2 代码轨（只在独立 worktree）

| 项 | 值 |
|------|------|
| Worktree | `C:\Users\Ariel\ccx-wt\codex-p0-provider-planning-preview` |
| 分支 | `codex/p0-provider-planning-preview` |
| 起点 / merge-base | `81856da`（与 `feature/skill-engine` HEAD 相同） |
| 当前 HEAD | `87f8040`（只有 Task 1） |
| SDD 账本 | `D:\ccx\.git\worktrees\codex-p0-provider-planning-preview\sdd\` |
| 共享 `D:\ccx` 代码 | **未改**，仍停在 `81856da` |
| Push / 合并 | **没有** |

基线（开工前，worktree 干净 checkout）：`npm run test` **373/373**，`npm run build:main` 通过。

---

## 2. 三片进度

| 片 | 合同内容 | 代码 | 提交 | 双重审查 | 备注 |
|----|----------|------|------|----------|------|
| Task 1 Provider | 请求级 Provider 快照，删 `aiService.configure` | 完成 | `87f8040` | 通过（无 Critical/Important） | 第四人核对见审查第八节 |
| Task 2 Planning | committed 快照 + 写库前 epoch | **工作区未提交草稿** | 无 | 未做 | 实现代理中途停了；缺最终门前几项 |
| Task 3 Preview | 写章预览去掉 `dangerouslySetInnerHTML` | 未开始 | — | — | worktree 里该注入仍在 |
| Task 4 整分支门 | 全量测试 + 三个 UI runner + 整分支审查 | 未开始 | — | — | 合同写明不是每片都跑 UI runner |

---

## 3. 结果：已经真正落地的能力（Task 1）

提交 `87f8040`：`fix: AI 请求改为调用瞬间 Provider 快照，去掉全局 configure`（14 文件，+428 −177）。

行为：

- 删除全局 `aiService.configure` 及 7 个调用点。
- `chat` / `chatStream` 每次接收完整 `ProviderConfig` 快照；`chatStream` 是**同步外壳**，调用瞬间拷贝配置再进入 generator。
- `projectId` 必填（trim 后非空）；主进程 `normalizeStreamProjectId` 只登记归一化值。
- 写章 `writeModel`、抽取 `summaryModel` 写入当次 `config.model`；渲染端不再有 `ChatCallOptions.model`。
- 主进程 `ChatOptions.model` **保留**（合同要求）。
- 未调用 `invalidateCache`，无迁移，未改写章预览 HTML。

实现方报告：全量 **386/386**（基线 373 + 本片 13）、`build:main`、`build:renderer` 通过。  
`rg "aiService.configure" src` 无匹配。三个 UI runner 按合同未跑。外部供应商烟测未做。

双重审查：**规格符合、质量 Approved。** Minor 记入最终审查清单（本片不改）：

1. 审稿 / 润色 / 素材各放一份精简 `PROVIDERS`，未抽共享模块。
2. 起名助手 init 失败没有显式把 ref 置空。
3. 批量写章在 `!requestBase` 时先写了进度再静默 return。

第四人核对（审查 **第八节**，定向测 21/21 已复跑）额外记下、不阻塞进 Planning：

- **8.2** `PlanningWorkspace.loadFirstAiConfig` 仍用 `providerId` 当 `name`，其它面板用 `preset?.name ?? providerId`。当前预设 `name === id`，功能不炸；将来 `name ≠ id` 会只有策划串供应商。建议 Planning 收尾或最终门统一。
- **8.3** `AIChatPanel` 新增配置从不持久化 `baseUrl`（既有 bug，Provider 片显形）。built-in 无回归；`custom` 供应商可能静默空 endpoint。**不要本片顺手修。**
- **8.4** renderer / 主进程缺 `projectId` 的报错文案不一致，用户只看得到 renderer 那句。无害。

---

## 4. Task 2 未提交草稿（不要当成已完成）

实现代理已在 worktree 写出大部分 Planning 代码，但：

- **没有 commit**
- **没有 `task-2-report.md`**
- **没有双重审查**
- **没有跑片末全量测试 / `build:main` / `build:renderer` 的书面记录**
- `tests/ui/obsidian-import.tsx` **未改**（短计划 Step 6 要求：commit 前 reserve、刷新失败留 refreshPending）
- `AGENTS.md` **未改**（短计划本片要求追加一条架构约束）
- App **没有** `planningLoadError` state（短计划 Step 7 要求）

`git status`（相对 `87f8040`）：

```
 M src/renderer/App.tsx
 M src/renderer/components/ObsidianImportPanel.tsx
 M src/renderer/components/PlanningWorkspace.tsx
 M src/renderer/services/planning-loader.ts
 M tests/unit/planning-loader.test.ts
?? src/renderer/services/planning-persistence.ts
?? src/renderer/services/planning-write-epoch.ts
?? tests/unit/planning-persistence.test.ts
?? tests/unit/planning-write-epoch.test.ts
```

已写进草稿、看起来对齐合同的部分：

- loader：`success:false` / 抛错只走 `onError`，不再 `onApply(null)`；`data === null` 仍是合法空策划；新增 `applyCommitted`（`generation+1` 作废在途 load）。
- `Map<projectId, number>` epoch：同一同步调用里比较 + 递增；失败不回滚。
- `persistPlanning`：invoke **之前** reserve；判别联合 `saved | superseded | failed`。
- 普通保存用 `db:planning:save` 返回的 `PlanningIdea` 立刻 `onPlanningCommitted` → `planningLoader.applyCommitted`。
- 长任务启动时 `capturePlanningWriteEpoch(startedId)`，写库带 `expectedEpoch`。
- Obsidian：触及策划时在 `guard.commit` 前 `reservePlanningWrite`；导入后同时 await 策划页 load、App reload、实体刷新。

下一位接手时：先在 worktree 里对照短计划 Task 2 Step 5～9 把缺口补完，再定向测 + 全量测 + 提交，再派审查代理。不要把这份草稿直接当绿灯。

---

## 5. 修改文件清单

### 5.1 共享 checkout `D:\ccx`（文档，未跟踪 / 未提交）

| 文件 | 作用 |
|------|------|
| `docs/superpowers/specs/2026-09-13-ai-production-current-contract.md` | 施工合同 |
| `docs/superpowers/plans/2026-09-13-p0-provider-planning-preview.md` | 三片实施计划 |
| `docs/superpowers/plans/2026-09-13-p0-provider-planning-preview-review.md` | 计划审查 + Task 1 核对 |
| `docs/superpowers/plans/2026-09-13-ai-long-novel-production-architecture-plan.md` | 冻结讨论档案 |
| `docs/superpowers/plans/2026-09-13-p0-status.md` | 本文 |

`CLAUDE.md` / `AGENTS.md` 里「P0 未施工」那一行已改为指向本文。这两份文件本身还有其它未提交改动，与 P0 代码无关。

### 5.2 worktree 已提交（Task 1，`87f8040`）

新增：

- `src/renderer/services/ai/request-config.ts`
- `tests/unit/ai-request-config.test.ts`
- `src/main/ai/stream-project-id.ts`
- `tests/unit/stream-project-id.test.ts`

修改：

- `src/renderer/services/ai.service.ts`
- `src/main/ipc/ai.ipc.ts`
- `tests/unit/ai-stream-cancel.test.ts`
- `AIChatPanel` / `AIWritePanel` / `AIReviewPanel` / `AIPolishPanel` / `ContextPanel` / `NameGenerator` / `PlanningWorkspace`

### 5.3 worktree 未提交（Task 2 草稿）

见第 4 节文件列表。

---

## 6. 明确没有做的事

- 没有数据库迁移；库版本仍应是 **v20**。
- 没有创建 P1《叙事时间模型设计》。
- 没有 as-of、分析 jobs、ContextAssembler、chapter-run 二/三期。
- 没有把 `hi-story-ai-configs` 改成按项目。
- 没有 Markdown 渲染预览。
- 没有把 PlanningWorkspace 全部字段抬进 App，没有 `planning_ideas.revision`。
- 没有 push，没有合进 `feature/skill-engine`。
- 共享 `D:\ccx` 里其它未提交工作（工作区 P3 报告、dist 等）与本 P0 **无关**，不要搅进同一 commit。

---

## 7. 下一步建议（按顺序，不要并行）

继续在 **同一个 worktree** 里做。三片都可能改 `AIWritePanel`，并行会抢文件。

### 立刻（Task 2 收口）

1. 打开 `C:\Users\Ariel\ccx-wt\codex-p0-provider-planning-preview`，不要在 `D:\ccx` 里续写。
2. 对照短计划 Task 2 **Step 5～9** 补缺口，至少包括：
   - `tests/ui/obsidian-import.tsx`：commit 前 reserve；刷新失败只重试、commit 一次。
   - App `planningLoadError`（失败不清 committed）。
   - 8.2 的 `name` 收敛（审查第八节建议带进本片）。
   - `AGENTS.md` 一条架构约束（只在 worktree 改，或合并时再改；不要和共享工作区脏文件缠在一起）。
3. 定向测：`planning-loader` / `planning-write-epoch` / `planning-persistence`。
4. 片末：`npm run test`、`npm run build:main`、`npm run build:renderer`。
5. 按短计划指定信息在 **worktree 内** commit，不 push。
6. 生成 diff 包，fresh reviewer 做规格符合性 + 代码质量；Critical/Important 修完再进 Task 3。

### 然后（Task 3）

按短计划切 `<p>` 为 React 文本节点；抽不出段时整份进 `<pre>`。保存路径仍是 HTML 原文。不改审稿/润色，不改写章 prompt。

### 然后（Task 4 整分支门）

合同命令：

```bash
npm run test
npm run build:main
npx vite build
rg "aiService\\.configure" src
rg "dangerouslySetInnerHTML" src
rg "invalidateCache" src/renderer
node tests/ui/run-writing-workspace.cjs
node tests/ui/run-obsidian-import.cjs
node tests/ui/run-creative-decision-ledger.cjs
```

过关后再做整分支审查。最终门待办（不是挡合并的新功能）：8.3 自定义供应商 `baseUrl`；Task 1 三条 Minor。

### 合并（必须等人拍板）

- worktree 绿灯 ≠ 可以合进脏的 `D:\ccx`。
- 目标分支 `feature/skill-engine` 上还有大量未提交文件。合并前先处理那些脏文件，或只把 `codex/p0-provider-planning-preview` 的提交 cherry-pick / merge 到干净状态。
- 合入后才改共享工作区的 `CLAUDE.md` / `AGENTS.md` 把 P0 标成「已落地」，并勾掉短计划 checkbox。
- 外部供应商烟测需单独授权。

### 不要做

- 不要开工 P1，不要写迁移，不要按讨论稿复活 v21/v22。
- 不要在共享 checkout 里把 Task 2 草稿再拷一份。
- 不要 push。

---

## 8. 给其他开发者的读法

| 问题 | 看哪 |
|------|------|
| P0 到底许了什么、禁了什么 | 合同 |
| 怎么一步步写 | 短计划（已不是旧版 7 个 Task） |
| 计划审查默认值有没有进计划 | 审查 7.7 / 7.8 |
| Provider 片代码靠不靠谱 | 审查第八节 + `sdd/task-1-report.md` |
| 现在能不能合并 | **不能。** 见本文第 2、7 节 |
| P1 能不能开 | **不能。** 需独立《叙事时间模型设计》授权 |

SDD 文件在 git 目录里，不随 worktree 工作副本走：

- `D:\ccx\.git\worktrees\codex-p0-provider-planning-preview\sdd\progress.md`
- `task-1-brief.md` / `task-1-report.md` / `task-1-review-package.diff.txt`
- `task-2-brief.md`（有 brief，无 report）
