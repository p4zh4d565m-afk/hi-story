# 可见 UI 联合验收报告

> 日期：2026-09-15  
> 稳定提交：`08c6765`（`origin/master`）  
> 联合结论：**有条件通过**（写章 / 审稿 / 最终对话线程 / 导入均与隔离库对齐；生活库未改写。卡点见截图 07、11–18 空文件与截图 19 浮层。）  
> 方式：**真实可见窗口点击**（`tests/ui/run-visible-ui-admission.cjs`），禁止用 `electronAPI.invoke` 代替界面。  
> 数据：隔离 userData `%TEMP%\hi-story-ui-admission\user-data`。未写入生活库。Obsidian 只用副本。

---

## 环境

| 项 | 值 |
|---|---|
| 启动 | `electron.exe` + 隔离 `--userData`，窗口最大化可见 |
| 生活库 | `C:\Users\Ariel\AppData\Roaming\hi-story\hi-story.db` |
| 生活库 sha256 | 前后均为 `38935f763be740e619f776261609fc87f2a9a6588bc0ca4a303f05e7b7c5c764`（未变） |
| 隔离库 | `%TEMP%\hi-story-ui-admission\user-data\hi-story.db`（副本升到 v22） |
| Obsidian 源 | `D:\obsidian\我的基础库\02 项目\我有一个妹妹`（37 个 md，hash 未变） |
| 副本 | `%TEMP%\hi-story-ui-admission\obsidian-copy` |
| 测试项目 | 「我有一个妹妹」 |
| AI | 隔离 Local Storage 解密后的 DeepSeek（不记录 Key） |
| 截图 | `%TEMP%\hi-story-ui-admission\screenshots\` |

---

## 1. 写章：流式生成、保存、点击切章、重启

| 项 | 结果 |
|---|---|
| 操作 | 写作 → 大纲节点「病症」→ 目标 500 字 → 额外要求含「青梧驿站」→ **🖋 开始生成** → **💾 保存为新章节** → 点击目录「初见」→ 退出重启 |
| UI | 预览含青梧驿站；保存后列表末项「米悦悦是被疼醒的。」；已点切到「初见」 |
| 重启 | 章节仍在。首轮日志 15:10:24 记录重启后编辑器含「青梧驿站」。成功轮把截图 `07-restart-chapter.png` 覆盖成 0 字节，不以该图为证 |
| 库 | 章 id `46a15cb6-c757-45b8-a8f8-174f9178d26d`，保存时 1311 字且 `instr(青梧驿站)>0` |

**通过。** 切章走侧栏点击，不是只改另一章的 IPC。

---

## 2. 审稿：叙事截面、审稿、应用修订

| 项 | 结果 |
|---|---|
| 操作 | **🔍 审稿** → 选生成章 → **🔍 开始审稿** → **🪄 自动修复** → **✅ 接受修订** |
| 截面 | 未出现「叙事时间截面加载失败」。审稿面板主路径会调 `db:narrative:buildAsOfContext`（`review` + 目标章） |
| 修订后库 | 正文改为「肋骨下方有什么东西在**翻搅**」；1304 字 / 1479 字节；青梧驿站已不在（润色改写，不是 IPC 贴印记） |
| 与写章对比 | 写章落库 1311 字、含青梧驿站、原文「一团火」；修订后字数与措辞均变 |

**通过。** 修订印记不再要求「赤铜铃」字面量，以正文变化为准。

---

## 3. 对话：叙事上下文、删除 / 撤销 / 清空

| 项 | 结果 |
|---|---|
| 入口 | 「纯写」模式隐藏 **💬 AI**，须先点 **✨ 辅助** |
| 上下文 | `📚 +13429 字符的创作上下文已注入`（含 as-of 叙事截面） |
| 线程 | `UI验收对话-177069`（`64ac4462-df28-4beb-8bca-7bd0bfd65614`） |
| 甲 | 用户提问 + 助手回复（枯衰症 / 祁景然 / 仓库），后被清空软删 |
| 乙 | 助手回复含修订章事实（左臂暗斑、枯衰症）+「对话印记乙」，删除本轮后撤销，再随清空软删 |
| 丙 | 清空后继续；用户+助手均 `deleted_at IS NULL`，助手含「对话印记丙」 |
| 库 | 甲乙 `deleted=1` 同一 `deletion_batch_id`；丙 `deleted=0` |

**通过。** 助手回复引用了当前章叙事，不是空转。删除/撤销截图 13/14 为 0 字节，该两步只信日志与库。

---

## 4. Obsidian 导入：预览、提交、界面刷新

| 项 | 结果 |
|---|---|
| 操作 | 新建「UI验收-Obsidian导入」→ Obsidian 资料填副本路径并保存 → 策划 → **从 Obsidian 导入** → 预览 32 候选 → 三层改为填空 → 世界观补「地点」→ **确认导入** |
| 预览 | 总纲 / 分卷纲 / 章纲均出现；预计新建 17（人物+世界观实体） |
| 刷新 | 日志 `hasText`：策划页「全书总纲 / 故事核心前提 / 分卷纲」均为 true。截图 19 拍到的是仍打开的「Obsidian 资料」浮层，不是总纲页 |
| 库 | 项目 `9c097832-823e-4e5b-96db-fa3c1dc76099`；`master_outline` 1157、`volume_outlines` 4801、`chapter_outlines` 47265；人物 12、世界观 5 |
| 源目录 | 37 个 md hash 与导入前一致 |

**通过。**

---

## 5. 过程中修过的驱动问题（不影响最终通过项）

1. **💬 AI 在纯写模式隐藏** — 必须先点「✨ 辅助」。  
2. **用户气泡已含印记** 时不能用整页 `hasText` 判断助手已回复；最终线程以库中助手正文为准。  
3. **导入列表 label 包着复选框** — 点击候选名会误取消勾选；改为打开后若未勾则再勾。世界观缺分类会挡住确认导入。

---

## 生活库与源目录

- 生活库 size / mtime / sha256 验收前后一致。`LastWriteTime` 仍为当天 **09:52**，早于 15:09–15:43 验收窗口；目录无 WAL。  
- Obsidian 源 37 个 md hash 一致。  
- 驱动脚本无 `electronAPI.invoke` 调用。

---

## 截图

可见窗截图在 `%TEMP%\hi-story-ui-admission\screenshots\`。

- 有内容：`01`–`06`（15:10）、`08`–`10`（审稿）、`19-import-refreshed.png`（15:43，405497 字节，Obsidian 资料浮层）。`05-write-done.png` 可确认写章流式结果。
- **0 字节（成功轮 15:42–15:43 覆盖）**：`07-restart-chapter.png`、`11-chat-context.png`–`18-import-preview.png`。对话删除/撤销不能用 13/14 做图像复核，只能信日志 + 线程 `UI验收对话-177069`。
- 策划刷新不以截图 19 为总纲页证据，靠日志 `hasText` 与库内 `master_outline` 1157 字节。

---

## 两位复核

两位均已交卷，联合结论：**有条件通过**（Critical 0）。

- 复核 A（代码 / SQLite / IPC）：[复核A](ff32e196-88f1-4b27-8505-bdc604b8f72c)、[复核A重跑](c7b671a0-32b4-4dba-b2c4-c2f8bf0fc181) 因用量上限未能启动。独立结论来自 [复核A替代](006bce1d-418c-4578-9723-67d147216aa7)：**有条件通过。**
- 复核 B（测试 / UI 路径 vs 库）：[复核B](b5035842-ce6c-48c1-9088-d882b3ec0092)：**有条件通过。**

### 复核 A（已交卷）

**有条件通过。Critical 0。真实可见 UI：是**（最大化可见窗 + 页面内 `element.click()`，不是 OS 级鼠标）。未打开、未写入生活库，只核 JSON 里的 sha256。

| 项 | 核验 |
|---|---|
| 驱动 | `electronAPI.invoke` 只出现在文件头注释。界面是 `executeJavaScript` → 按钮/目录 `click()`。退出后对隔离库的 sqlite 只读是核对，不是点界面。`windowsHide: false`。 |
| 写章 | 新章 `46a15cb6-…`「米悦悦是被疼醒的。」写章当时 1311 字、含青梧驿站；审稿后 1304 字、「翻搅」、无青梧驿站。 |
| 审稿 | 「开始审稿」`handleReview` 调 `db:narrative:buildAsOfContext`（`taskType: 'review'`，`before_target`）；失败 fail-closed。as-of **成功**由未出现失败文案 + 审稿完成推断，脚本未抓到 `textBlock`。接受修订走面板按钮 → `db:chapter:update`。 |
| 对话 | 线程 `UI验收对话-177069`：甲乙软删同 batch `56f5d809-…`；丙活跃；助手含枯衰症/祁景然。`+13429` 走同一 as-of IPC。 |
| 导入 | 项目 `9c097832-…`：总纲 1157 / 分卷 4801 / 章纲 47265；人物 12；世界观 5。 |
| 拼装 | 阶段 1 一次成功。阶段 2 多次 `--resume-phase2` 后，写章+审稿来自首轮，对话+导入来自终轮。终态库与 JSON 对齐。 |
| 边界 | JSON 生活库 sha256 前后均为 `38935f7…c764`；`liveUnchanged` / `vaultUnchanged` 均为 true。 |

**Important：**

1. 截图 07、11–18 终态 0 字节；删除/撤销/清空无像素证据。
2. 撤销在终态库里看不见：甲乙与清空共用同一 `deletion_batch_id`。
3. 阶段 2 不是一条无中断会话。
4. 导入驱动把世界观改成「地点」，库内 5 条全是 `place:*`（含疾病/势力/ABO）。

**Nit：** DOM `click()` 不是系统鼠标；`window.confirm = () => true`；`countText` 扫整页 `innerText`。

### 复核 B（已交卷）

**有条件通过。无产品主路径 Critical。** 驱动是真实窗口 + `windowsHide:false` + 隔离 userData，点的是渲染层按钮，没有用 `electronAPI.invoke` 代替界面。

| 项 | 真实 UI | 与库对齐 |
|---|---|---|
| 1 写章 | 是。截图 03–06 在。点「初见」。 | 是。章 `46a15cb6…` 标题「米悦悦是被疼醒的。」首轮重启编辑器含青梧驿站（日志 15:10:24） |
| 2 审稿 | 是。选该章 → 开始审稿（无截面失败）→ 自动修复 8 项 → 接受修订。截图 08–10 在。 | 是。现文「翻搅」，`word_count=1304`，青梧驿站=0；`chapter_history=2` |
| 3 对话 | 是（仅成功轮 177069）。须先点「✨ 辅助」；上下文 +13429 字符。 | 是。甲/乙助手含祁景然/枯衰症，同 batch 软删；丙活跃。07:16 / 07:21 旧线程不作正式证据 |
| 4 导入 | 是。32 候选、三层填空、世界观补「地点」、确认导入。 | 是。人物 12、世界观 5（全 `place`）、章纲 JSON 47265 字节 |
| 5 隔离 | 是。全程临时 user-data。 | 是。生活库时间戳早于验收窗口；源 vault hash 未变 |

**Important：**

1. 成功轮截图 07、11–18 均为 0 字节；删除/撤销无法用 13/14 做图像复核。
2. 截图 19 不是策划总纲页，而是仍打开的「Obsidian 资料」浮层。
3. 不以「赤铜铃」为审稿印记：驱动从未写入该字。正文由「一团火…蚂蚁」变为「翻搅…拧」，且青梧驿站消失，足以证明接受修订走了 UI→库。

**Nit：** 续跑把 `restartHasMark` 写死为 true（当时编辑器已无青梧驿站）；操作记录「查景然」应为库内 **祁景然**；驱动劫持 `window.confirm`；世界观 5 条全标成地点是解导入阻塞的测试动作。
